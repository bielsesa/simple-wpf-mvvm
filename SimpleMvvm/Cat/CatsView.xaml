<UserControl x:Class="SimpleMvvm.Cat.CatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SimpleMvvm.Cat"
             xmlns:converters="clr-namespace:SimpleMvvm.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:CatsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

  <UserControl.DataContext>
    <local:CatsViewModel />
  </UserControl.DataContext>

  <UserControl.Resources>
    <converters:CatColorToBrushConverter x:Key="CatColorToBrushConverter"/>
    <converters:CatColorToStringConverter x:Key="CatColorToStringConverter"/>
    <converters:StrokeThicknessFromCatColorConverter x:Key="StrokeThicknessFromCatColorConverter"/>
  </UserControl.Resources>

  <StackPanel Orientation="Vertical">
    <StackPanel Orientation="Horizontal" Margin="5">
      <Rectangle 
        Fill="{Binding CatColor, Converter={StaticResource CatColorToBrushConverter}}" 
        Stroke="Black" 
        StrokeThickness="{Binding CatColor, Converter={StaticResource StrokeThicknessFromCatColorConverter}}" 
        Height="50" 
        Width="50"
        RadiusX="5"
        RadiusY="5"
        />
      <StackPanel Orientation="Vertical" Margin="5">
        <TextBlock Text="{Binding CatName}"
                   FontWeight="Bold"/>
        <TextBlock Text="{Binding CatBirthDate, StringFormat='{}{0:MMMM dd, yyyy}'}"
                   Foreground="Gray"/>
        <TextBlock Text="{Binding CatColor, Converter={StaticResource CatColorToStringConverter}}"/>
      </StackPanel>
    </StackPanel>
    <StackPanel Orientation="Horizontal" Margin="5">
      <Button Command="{Binding Previous}" Content="Previous" Margin="2" />
    <Button Command="{Binding Next}" Content="Next" Margin="2" />
    </StackPanel>
</StackPanel>

</UserControl>
